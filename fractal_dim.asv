radii=60:5:100; radii=cat(radii,radii,1);
k=1;
mass=zeros(size(radii));

for j=1:length(radii)
    [mass(j),A]=dla2D(radii(j),k);
end

logr=log(radii);
logm=log(mass);
hold on;
scatter(logr,logm,'o')
re=polyfit(logr,logm,1);
plot(logr, polyval(re,logr))
hold off;